build:
  builder: nixpacks
  buildCommand: pip install -r requirements.txt

deploy:
  startCommand: python main.py
  healthcheckPath: /manual/health
  healthcheckTimeout: 60

variables:
  - name: PORT
    value: 5000
  - name: PYTHONUNBUFFERED
    value: "true"
