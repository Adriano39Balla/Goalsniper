services:
  - name: goalsniper
    start: "python main.py"
    # No cron needed; APScheduler runs inside the process
    envs:
      - key: TZ
        value: Europe/Berlin
      - key: APIFOOTBALL_BUDGET_DAILY
        value: "2200"
