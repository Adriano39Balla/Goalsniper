# Core (essential)
Flask==3.0.0
# PATCH: psycopg2-binary==2.9.9 predates Python 3.13 support (added in
# 2.9.12 per the official psycopg release notes — 2.9.9 doesn't know about
# the 3.13 C-API changes, hence the _PyInterpreterState_Get compile error).
# Same fix pattern as numpy/pandas/scikit-learn below: floor instead of
# exact-pin so pip resolves to a version with an actual prebuilt wheel.
psycopg2-binary>=2.9.9,<3.0
requests==2.31.0

# ML Core (minimal)
# Exact pins (numpy==1.26.3, pandas==2.2.0, scikit-learn==1.4.0) had no
# prebuilt wheels for Python 3.13 — pandas only got cp313 wheels starting
# at 2.2.3. Ranges let pip's resolver pick whatever mutually compatible,
# wheel-available versions match whatever Python Railway builds with.
numpy>=1.26.3,<3.0
scikit-learn>=1.4.0,<2.0
pandas>=2.2.3,<3.0

# Scheduling & time
APScheduler==3.10.4
python-dateutil==2.8.2
pytz==2023.3.post1

# Environment & HTTP
python-dotenv==1.0.0
urllib3==2.1.0

# Server (for Railway/Render)
gunicorn==21.2.0
